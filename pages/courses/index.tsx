import React from 'react';
import { CoursesPage, MainLayout } from '../../components';

const Courses: React.FC = () => (
  <MainLayout title="Courses">
    <CoursesPage />
  </MainLayout>
);

export default Courses;
