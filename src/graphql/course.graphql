query CourseDetail($id: ID) {
    course(id: $id) {
        id
        name
        code
        modified
        created
        score
        resourceCount
        starred
        vote {
            id
            status
        }
        subject {
            id
            name
        }
        school {
            id
            name
        }
        user {
            id
            username
        }
        resources {
            id
            title
            score
            date
        }
        comments {
            id
            user {
                id
                username
                avatarThumbnail
            }
            text
            attachment
            modified
            created
            replyComments {
                id
                user {
                    id
                    username
                    avatarThumbnail
                }
                text
                attachment
                modified
                created
                score
                vote {
                    id
                    status
                }
            }
            replyCount
            score
            vote {
                id
                status
            }
        }
    }
}

mutation DeleteCourse($id: ID) {
    deleteCourse(input: { id: $id }) {
        message
        errors {
            field
            messages
        }
    }
}
