query CourseDetail($id: Int!) {
    course(courseId: $id) {
        id
        name
        code
        subject {
            id
            name
        }
        school {
            id
            name
        }
        user {
            id
            username
        }
        resources {
            id
            title
            points
        }
        modified
        created
        points
        resourceCount
    }
}
