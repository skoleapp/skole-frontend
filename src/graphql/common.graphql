query Schools {
    schools {
        id
        name
    }
}

query SchoolTypes {
    schoolTypes {
        id
        name
    }
}

query Courses {
    courses {
        id
        name
    }
}

query Subjects {
    subjects {
        id
        name
    }
}

query Countries {
    countries {
        id
        name
    }
}

query Cities {
    cities {
        id
        name
    }
}

query ResourceTypes {
    resourceTypes {
        id
        name
    }
}

mutation CreateComment(
    $text: String!
    $attachment: String
    $course: ID
    $resource: ID
    $resourcePart: ID
    $comment: ID
) {
    createComment(
        input: {
            text: $text
            attachment: $attachment
            course: $course
            resource: $resource
            resourcePart: $resourcePart
            comment: $comment
        }
    ) {
        comment {
            id
            user {
                id
                username
                avatarThumbnail
            }
            text
            attachment
            modified
            created
            replyComments {
                id
                user {
                    id
                    username
                    avatarThumbnail
                }
                text
                attachment
                modified
                created
                points
                vote {
                    id
                    status
                }
            }
            replyCount
            points
            vote {
                id
                status
            }
        }
        errors {
            field
            messages
        }
    }
}
