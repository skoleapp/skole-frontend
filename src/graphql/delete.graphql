mutation DeleteObject($comment: ID, $resource: ID, $resourcePart: ID, $vote: ID) {
    deleteObject(
        input: {
            comment: $comment
            resource: $resource
            resourcePart: $resourcePart
            vote: $vote
        }
    ) {
        message
        errors {
            field
            messages
        }
    }
}
