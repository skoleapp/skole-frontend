query SchoolDetail($id: ID) {
    school(id: $id) {
        id
        name
        city
        country
        schoolType
        subjects {
            id
            name
        }
        courses {
            id
            name
            code
            score
        }
        subjectCount
        courseCount
    }
}
