// These are the only valid page refs, i.e. dynamic back buttons work only with these.
// Values must have a matching pattern with the corresponding URL.
export enum PageRef {
  GET_STARTED = 'get-started',
  FOR_TEACHERS = 'for-teachers',
  HOME = 'home',
  SEARCH = 'search',
  COURSE = 'course',
  RESOURCE = 'resource',
  PROFILE = 'profile',
  ABOUT = 'about',
}
