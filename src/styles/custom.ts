import { makeStyles } from '@material-ui/core';

export const useCustomStyles = makeStyles({
    mainAvatar: {
        height: '8rem',
        width: '8rem',
    },
    avatarThumbnail: {
        height: '1.5rem',
        width: '1.5rem',
    },
});
